query($id: ID!) {
    book(id: $id) {
        id
        title
        author
        image
        link
        description
        featured
        category {
            id
            name
        }
    }
}
